<h1>Lista de usuarios</h1>

<table class="users-table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Email</th>
      <th>Admin</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.admin? ? "Sí" : "No" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

